import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'poesias-Pelaez-Juan',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
